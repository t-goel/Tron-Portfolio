---
phase: 04-sector-dives-finish
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/projects.js
  - src/data/contact.js
autonomous: false
gap_closure: true
requirements:
  - PROJ-02
  - PROJ-05
  - PROJ-06
  - ABOUT-04
must_haves:
  truths:
    - "Projects sector displays real project names, taglines, and tech stacks — no 'TBD' strings visible"
    - "VIEW ON GITHUB links navigate to valid GitHub repository URLs"
    - "About terminal contact section renders clickable GitHub, LinkedIn, and email links — no '[not set]' text"
  artifacts:
    - path: "src/data/projects.js"
      provides: "Real project data for all three entries"
      contains: "name:"
    - path: "src/data/contact.js"
      provides: "Real contact information"
      contains: "github:"
  key_links:
    - from: "src/components/UI/ProjectsSector.jsx"
      to: "src/data/projects.js"
      via: "import { projects }"
      pattern: "name:.*[^'TBD']"
    - from: "src/components/UI/AboutSector.jsx"
      to: "src/data/contact.js"
      via: "import { contact }"
      pattern: "github:.*[^'TBD']"
---

<objective>
Close the two remaining verification gaps in Phase 4 by replacing all 'TBD' placeholder values in projects.js and contact.js with the user's real content.

Purpose: The portfolio infrastructure is complete but renders placeholder data — a recruiter would see 'TBD' for every project name and broken GitHub links. This plan collects real data from the user and fills it in.
Output: Two updated data files with real content; zero 'TBD' strings remaining.
</objective>

<execution_context>
@/Users/tanmaygoel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaygoel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sector-dives-finish/04-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Collect real project and contact data from user</name>
  <read_first>src/data/projects.js, src/data/contact.js</read_first>
  <decision>The user must provide their real portfolio content to replace all 'TBD' placeholders.</decision>
  <context>
Both src/data/projects.js and src/data/contact.js contain 'TBD' for every content field. The portfolio renders correctly but shows placeholder text and broken links. The executor cannot guess this information — it must come from the user.
  </context>
  <options>
    <option id="provide-data">
      <name>Provide all data now</name>
      <pros>Single interaction, plan completes in one pass</pros>
      <cons>User needs all info ready</cons>
    </option>
  </options>
  <action>
Ask the user to provide the following information:

**For each of the 3 projects (projects.js already has 3 slots):**
1. Project name
2. One-line tagline/description
3. Tech stack (list of technologies, e.g. ["React", "Node.js", "PostgreSQL"])
4. GitHub URL (full URL like https://github.com/user/repo)
5. Which project (if any) is currently active/in-progress? (gets Crimson Red accent + "IN PROGRESS" badge; others get Neon Orange accent)

**Contact info:**
1. GitHub handle or full URL (e.g., "tanmaygoel" or "https://github.com/tanmaygoel")
2. LinkedIn handle or full URL (e.g., "tanmaygoel" or "https://linkedin.com/in/tanmaygoel")
3. Email address

**Optional:** The About terminal bio in AboutSector.jsx is hardcoded (not from a data file). Ask the user if they want any changes to the bio text (whoami, background, interests, looking_for sections). If yes, note the changes for Task 2.

Present the current file structures so the user knows exactly what to fill in.
  </action>
  <resume-signal>User provides project details, contact info, and optional bio updates</resume-signal>
  <acceptance_criteria>
    - User has provided name, tagline, techStack array, and githubUrl for all 3 projects
    - User has provided github, linkedin, and email contact values
    - All GitHub URLs start with "https://"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Populate data files with real content</name>
  <read_first>src/data/projects.js, src/data/contact.js</read_first>
  <files>src/data/projects.js, src/data/contact.js</files>
  <action>
Using the exact data provided by the user in Task 1, update both files:

**src/data/projects.js:**
- Replace each project's `name: 'TBD'` with the real project name string
- Replace each project's `tagline: 'TBD'` with the real tagline string
- Replace each project's `techStack: ['TBD']` with the real array of technology strings
- Replace each project's `githubUrl: 'TBD'` with the real GitHub URL string (must start with https://)
- Set `active: true` ONLY on the project the user identified as in-progress; remove or set `active: false` on others
- Preserve existing fields: id, accentColor, position — do NOT change these

**src/data/contact.js:**
- Replace `github: 'TBD'` with the user's GitHub handle or URL
- Replace `linkedin: 'TBD'` with the user's LinkedIn handle or URL
- Replace `email: 'TBD'` with the user's email address

**If the user requested bio changes in AboutSector.jsx:** Update the relevant TERMINAL_SEQUENCE entries in src/components/UI/AboutSector.jsx. Only modify the output text strings, not the command names or animation logic.

Do NOT change any other files. Do NOT modify the export structure, field names, or array length.
  </action>
  <verify>
    <automated>grep -c "'TBD'" src/data/projects.js src/data/contact.js</automated>
  </verify>
  <done>
    - grep for 'TBD' in both files returns 0 matches each
    - All githubUrl values start with "https://"
    - projects.js still exports an array of exactly 3 objects
    - contact.js still exports an object with github, linkedin, email keys
    - npm run build succeeds without errors
  </done>
  <acceptance_criteria>
    - grep -c "'TBD'" src/data/projects.js returns 0
    - grep -c "'TBD'" src/data/contact.js returns 0
    - grep -c "https://" src/data/projects.js returns at least 3 (one per project githubUrl)
    - grep -c "github:" src/data/contact.js returns 1
    - grep -c "linkedin:" src/data/contact.js returns 1
    - grep -c "email:" src/data/contact.js returns 1
    - npm run build exits with code 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep -rn "TBD" src/data/projects.js src/data/contact.js` returns no matches
2. `npm run build` completes successfully
3. Each project entry in projects.js has a name that is not 'TBD', a tagline that is not 'TBD', a techStack array with real technology names, and a githubUrl starting with 'https://'
4. contact.js has non-empty, non-TBD values for github, linkedin, and email
</verification>

<success_criteria>
- Zero 'TBD' strings in src/data/projects.js and src/data/contact.js
- All GitHub URLs are valid (start with https://)
- Production build succeeds
- Projects sector renders real project names and working GitHub links
- About terminal contact section renders clickable links instead of '[not set]'
</success_criteria>

<output>
After completion, create `.planning/phases/04-sector-dives-finish/04-05-SUMMARY.md`
</output>
