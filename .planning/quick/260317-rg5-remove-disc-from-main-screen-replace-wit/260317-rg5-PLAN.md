---
phase: quick
plan: 260317-rg5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
  - src/components/Scene.jsx
  - src/components/UI/EnterButton.jsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 2 main hub shows NO 3D disc — only the title, subtitle, and a full-screen centered ENTER THE GRID button"
    - "The button text cycles through random characters before resolving to ENTER THE GRID (glitch/decode animation)"
    - "Clicking the button calls setPhase(3) to advance to grid world"
    - "IdentityDisc.jsx component file is untouched — preserved for future nav bar reuse"
  artifacts:
    - path: "src/components/UI/EnterButton.jsx"
      provides: "Full-screen centered CTA with glitch text animation"
    - path: "src/App.jsx"
      provides: "Phase 2 renders EnterButton + TitleOverlay, no disc hover state"
    - path: "src/components/Scene.jsx"
      provides: "Phase 2 no longer renders IdentityDisc"
  key_links:
    - from: "src/components/UI/EnterButton.jsx"
      to: "src/store/appState.js"
      via: "setPhase(3) on click"
      pattern: "setPhase\\(3\\)"
---

<objective>
Remove the Identity Disc 3D scene from the phase 2 main hub. Replace it with a full-screen centered "ENTER THE GRID" button featuring a glitch/decode text randomization animation (characters cycle randomly before resolving to final text). Keep IdentityDisc.jsx intact for future nav bar use.

Purpose: Simplify the main hub landing to a bold CTA, removing the 3D disc which will be repurposed as a nav element later.
Output: Updated App.jsx, Scene.jsx, and EnterButton.jsx
</objective>

<execution_context>
@/Users/tanmaygoel/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanmaygoel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/App.jsx
@src/components/Scene.jsx
@src/components/UI/EnterButton.jsx
@src/components/UI/TitleOverlay.jsx
@src/components/3D/IdentityDisc.jsx
@src/store/appState.js

<interfaces>
<!-- Current EnterButton: simple button at bottom: 12%, calls setPhase(3) on click -->
<!-- Current Scene: renders IdentityDisc when phase===2, with Bloom + Vignette post-processing -->
<!-- Current App: passes discHovered state between Scene and TitleOverlay for glitch effect -->
<!-- TitleOverlay: has existing glitch char randomization logic (GLITCH_CHARS) we can reference for the button animation pattern -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glitch/decode text animation to EnterButton and center it full-screen</name>
  <files>src/components/UI/EnterButton.jsx</files>
  <action>
Rewrite EnterButton to be full-screen centered (not bottom-positioned) with a glitch/decode text animation:

1. Position the button dead center of the viewport (position: absolute, top: 50%, left: 50%, transform: translate(-50%, -50%)).
2. Make button larger — increase font size to clamp(1.2rem, 3vw, 2rem), padding to 1.2rem 3.5rem.
3. Add a glitch/decode text animation on mount using useEffect + useState + useRef:
   - Target text: "ENTER THE GRID"
   - On mount, start with all characters randomized (use chars from "0123456789ABCDEF!@#$%^&*").
   - Every 40ms tick, progressively "resolve" characters left-to-right: each character gets ~3-5 random cycles before locking to its final letter. Spaces resolve immediately.
   - After all characters resolved (~1.5s total), stop.
   - On hover, re-trigger a brief glitch burst (randomize 30-40% of chars for ~400ms then snap back) similar to TitleOverlay's pattern.
4. Keep the existing onClick={() => setPhase(3)} behavior.
5. Keep the red color scheme (#FF0000 / rgba(255,0,0,...)) and TR2N font.
6. Add a subtle pulsing glow animation on the border using CSS keyframes (pulse between rgba(255,0,0,0.2) and rgba(255,0,0,0.6) over 2s, infinite).
  </action>
  <verify>
    <automated>cd /Users/tanmaygoel/CS/Tron-Portfolio && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>EnterButton renders centered with glitch decode animation on mount, hover re-glitch, and pulsing border glow. Clicking calls setPhase(3).</done>
</task>

<task type="auto">
  <name>Task 2: Remove disc from Scene and clean up App.jsx disc hover wiring</name>
  <files>src/App.jsx, src/components/Scene.jsx</files>
  <action>
1. In Scene.jsx:
   - Remove the IdentityDisc import.
   - Remove the {phase === 2 && <IdentityDisc ... />} block entirely.
   - Remove the onDiscHover prop from the component signature (no longer needed).
   - Keep the lights, EffectComposer, Bloom, and Vignette — they provide ambient atmosphere for phase 2 background.

2. In App.jsx:
   - Remove the discHovered state (useState(false)) and its setter.
   - Remove the onPointerMissed handler from Canvas (it only served disc hover reset).
   - Update <Scene /> — remove the onDiscHover prop.
   - Update <TitleOverlay /> — remove the glitch={discHovered} prop, just pass glitch={false} or remove the prop entirely (component defaults to no glitch since glitch=false is the default behavior).
   - Keep the EnterButton render at {phase === 2 && <EnterButton />}.
   - Keep TitleOverlay render at {phase >= 2 && <TitleOverlay visible={phase === 2} />}.

DO NOT touch src/components/3D/IdentityDisc.jsx — it must remain intact for future nav bar reuse.
  </action>
  <verify>
    <automated>cd /Users/tanmaygoel/CS/Tron-Portfolio && npm run build 2>&1 | tail -5 && echo "---" && grep -c "IdentityDisc" src/components/Scene.jsx; grep -c "discHovered" src/App.jsx; test -f src/components/3D/IdentityDisc.jsx && echo "IdentityDisc.jsx preserved"</automated>
  </verify>
  <done>Scene no longer renders IdentityDisc in phase 2. App.jsx has no disc hover state. IdentityDisc.jsx file still exists untouched. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no errors
2. `grep -r "IdentityDisc" src/components/Scene.jsx` returns nothing
3. `grep -r "discHovered" src/App.jsx` returns nothing
4. `test -f src/components/3D/IdentityDisc.jsx` confirms file preserved
5. Visual: dev server shows phase 2 with centered glitch-animated ENTER THE GRID button, no 3D disc
</verification>

<success_criteria>
- Phase 2 displays a full-screen centered "ENTER THE GRID" button with glitch/decode text animation
- No Identity Disc rendered in phase 2
- Button click advances to phase 3
- IdentityDisc.jsx component file untouched
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260317-rg5-remove-disc-from-main-screen-replace-wit/260317-rg5-SUMMARY.md`
</output>
